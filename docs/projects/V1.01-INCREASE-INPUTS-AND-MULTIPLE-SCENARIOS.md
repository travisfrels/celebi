# V1.01 Increase Inputs and Multiple Scenarios

| Status | Set On |
|--------|--------|
| Planning | 2026-03-14 |
| Active | 2026-03-14 |
| Done | 2026-03-14 |

## Context

### Situation

Celebi is a Tkinter desktop app (`src/app.py`, `src/probability_engine.py`) that calculates exact dice probabilities for Trench Crusade. Current constraints:
- **Pool size**: 1–6 (validated in `_validate_inputs`, spinbox range 2–6 in UI)
- **Pick count**: 1–3 (validated in engine, but UI only exposes 2 and 3 via radio buttons)
- **Algorithm**: Brute-force enumeration via `itertools.product(range(1,7), repeat=pool_size)` — O(6^N). At N=6, this is 46,656 outcomes (fast). At N=12, this is ~2.2 billion (infeasible).
- **UI**: Single scenario with one config panel (left) and one results table (right) in a 600x400 window
- **No success/failure summary**: The table shows per-total exact and cumulative probabilities but no aggregate threshold display

### Opportunity

The user needs larger dice pools (up to 12 dice), wider pick counts (1–4), a quick-read success/failure indicator with configurable threshold (default 7), and side-by-side scenario comparison (up to 4 scenarios). The current algorithm cannot scale to pool size 12, and the UI only supports a single configuration.

### Approach

Three changes are required:

1. **Replace the probability engine algorithm** with multiset enumeration using `itertools.combinations_with_replacement`. Instead of enumerating all 6^N permutations, enumerate unique sorted outcomes (multisets) and weight each by its multinomial coefficient. At N=12, this reduces 2.2B outcomes to 6,188 unique multisets — a ~350,000x reduction. Results remain exact.

2. **Add success/failure probability calculation** — a function that aggregates exact probabilities below and at/above a configurable threshold (default 7).

3. **Refactor the UI for multiple scenarios** — extract config+results into a reusable scenario panel, support 1–4 panels side by side with add/remove controls, display success/failure summary per scenario, change pick count to spinbox (1–4), expand pool size to 1–12.

#### Alternatives not chosen

- **Dynamic Programming** — Equal performance but higher implementation complexity and correctness risk for the top/bottom-K selection problem. The multiset space at N=12 is only 6,188, so DP's advantage of avoiding enumeration entirely provides no practical benefit.
- **Brute-force with higher cap** — Capping at pool_size=8 doesn't meet the N=12 requirement. Even N=10 (~60M outcomes) introduces noticeable latency.
- **Fixed success threshold (7)** — Always showing failure as 1–6 and success as 7+. A configurable threshold adds negligible implementation cost while supporting house rules and variants.
- **Fixed 2 scenarios** — Covers the user's stated example but limits more complex tactical analysis. Up to 4 with add/remove is more flexible without risking UI clutter.
- **Summary row in table** — Mixes aggregate and per-value data; a separate display above the table is more prominent and scannable.

#### Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Algorithm | Multiset enumeration | O(C(N+5,5)) — 6,188 multisets at N=12 vs 2.2B brute-force outcomes. See [ADR-OP-PROBABILITY-CALCULATION-MULTISET-20260314](../adrs/ADR-OP-PROBABILITY-CALCULATION-MULTISET-20260314.md). |
| Pick count UI | Spinbox (1–4) | Radio buttons don't scale for 4 options; spinbox is consistent with pool size control |
| Success threshold | Configurable spinbox (default 7) | Trivial implementation cost, supports house rules |
| Multi-scenario layout | Side-by-side columns (up to 4) | Enables visual comparison; add/remove buttons for flexibility |
| Success/failure display | Separate element above table | More prominent and scannable than a summary row in the table |

## Goals

- Support pool sizes 1–12 and pick counts 1–4
- Display success/failure probability summary per scenario (configurable threshold, default 7)
- Allow up to 4 simultaneous scenarios for side-by-side comparison
- Replace the brute-force algorithm with multiset enumeration to handle larger pools
- Maintain exact combinatorial calculation (no simulation)

## Non-Goals

- Configurable dice types (still d6 only)
- Persistent storage / export of results
- Graphical charts or visualizations beyond the table
- Network/multiplayer features
- Full game rules engine (wounds, saves, morale, etc.)

## Exit Criteria

- [x] Pool size spinbox allows 1–12; pick count spinbox allows 1–4 (constrained to not exceed pool size)
- [x] Probability engine handles pool_size=12, pick_count=4 with correct exact probabilities in under 1 second
- [x] Each scenario displays a success/failure summary (configurable threshold, default 7) showing aggregated probability below and at/above the threshold
- [x] User can add up to 4 scenarios and remove any scenario (minimum 1 remains)
- [x] Multiple scenarios display side by side with independent configurations
- [x] All existing tests continue to pass
- [x] New tests cover: pool sizes 7–12, pick count 4, success/failure calculation, multi-scenario UI interactions

## References

- [V1.01 Increase Inputs and Multiple Scenarios Milestone](https://github.com/travisfrels/celebi/milestone/2)
- [#18 Create V1.01 Increase Inputs and Multiple Scenarios project](https://github.com/travisfrels/celebi/issues/18)
- [#19 Replace probability engine algorithm with multiset enumeration](https://github.com/travisfrels/celebi/issues/19)
- [#20 Add success/failure probability calculation](https://github.com/travisfrels/celebi/issues/20)
- [#21 Refactor UI to support multiple scenarios](https://github.com/travisfrels/celebi/issues/21)

### Follow-Up Issues

- [#29 [Post-Mortem] Add exit criteria verification step to project completion workflow](https://github.com/travisfrels/celebi/issues/29)
- [#30 [Post-Mortem] Enforce clean skill file paths during project creation](https://github.com/travisfrels/celebi/issues/30)

### Pull Requests

- [#24 Replace probability engine with multiset enumeration](https://github.com/travisfrels/celebi/pull/24)
- [#25 Add success/failure probability calculation](https://github.com/travisfrels/celebi/pull/25)
- [#26 Refactor UI to support multiple scenarios](https://github.com/travisfrels/celebi/pull/26)
- [#31 Add post-mortem for V1.01 Increase Inputs and Multiple Scenarios](https://github.com/travisfrels/celebi/pull/31)

### Design References

- [Python itertools.combinations_with_replacement](https://docs.python.org/3/library/itertools.html#itertools.combinations_with_replacement)
- [Multinomial coefficient (Wikipedia)](https://en.wikipedia.org/wiki/Multinomial_theorem#Multinomial_coefficients)
- [Tkinter documentation (Python docs)](https://docs.python.org/3/library/tkinter.html)
- [ADR-OP-PROBABILITY-CALCULATION-MULTISET-20260314](../adrs/ADR-OP-PROBABILITY-CALCULATION-MULTISET-20260314.md)

## Post-Mortem

The V1.01 project was completed in a single session (~58 minutes elapsed). All seven exit criteria were met with zero scope changes and zero rework iterations across all PRs. The project delivered three well-sequenced changes — algorithm replacement, success/failure calculation, and UI refactor — each building on the prior. PR reviews were thorough but found no defects in the implementation work itself. The V1.00 post-mortem recommendation to add exact probability display (#13) was incorporated into the V1.01 scope via the UI refactor.

### Timeline

| When | Event |
|------|-------|
| 2026-03-15 03:00 | Issues #19, #20, #21 created under V1.01 milestone |
| 2026-03-15 03:02 | PR #22 created — project definition, ADR, skills, milestone |
| 2026-03-15 03:17 | PR #22 merged (review found incorrect path references in bundled skill files) |
| 2026-03-15 03:26 | PR #24 created — multiset enumeration algorithm |
| 2026-03-15 03:33 | PR #24 merged, Issue #19 closed (clean review, no rework) |
| 2026-03-15 03:37 | PR #25 created — success/failure probability calculation |
| 2026-03-15 03:41 | PR #25 merged, Issue #20 closed (clean review, no rework) |
| 2026-03-15 03:55 | PR #26 created — UI refactor for multiple scenarios |
| 2026-03-15 03:59 | PR #26 merged, Issue #21 closed (clean review, no rework) |

### Impact

- **Milestone duration**: ~58 minutes elapsed (03:00 → 03:59 UTC). Note: cycle time is elapsed time, not active work time.
- **Issue cycle times** (created → closed):
  - Issue #19 (multiset algorithm): 32 minutes
  - Issue #20 (success/failure): 40 minutes
  - Issue #21 (UI refactor): 57 minutes
- **PR cycle times** (created → merged):
  - PR #22 (project definition): 15 minutes (1 review, observations on bundled skill file paths)
  - PR #24 (multiset algorithm): 7 minutes (1 review, clean)
  - PR #25 (success/failure): 4 minutes (1 review, clean)
  - PR #26 (UI refactor): 3 minutes (1 review, clean)
- **PR review rework**: 0 of 4 PRs required rework iterations — all reviews approved without changes
- **Scope changes**: None. All 3 planned implementation issues were completed as originally scoped
- **Test growth**: Test suite grew from 17 tests (V1.00) to 93 tests (35 engine + 58 UI)

### What Went Well

- **Zero rework across all PRs** — Every implementation PR was approved on first review. This suggests the project planning phase (issue decomposition, alternative analysis, ADR) front-loaded decision-making effectively, leaving no ambiguity during implementation.
- **Well-sequenced issue decomposition** — The three issues were ordered by dependency: algorithm first (unblocked everything), then success/failure (pure engine addition), then UI refactor (consumed both prior changes). This eliminated integration risk and allowed each PR to be small and focused.
- **Algorithm choice validated** — The multiset enumeration approach delivered pool_size=12, pick_count=4 in ~8ms, well under the 1-second exit criterion. The ~350,000x reduction from brute-force was realized as predicted in the project definition.
- **Comprehensive test coverage** — Test count grew from 17 to 93. Cross-validation tests (multiset vs brute-force for pool sizes 1–6) provided a strong correctness guarantee for the algorithm change. UI tests covered add/remove, constraint enforcement, and error handling.
- **V1.00 post-mortem recommendation incorporated** — The exact probability display recommendation (#13) from the V1.00 post-mortem was addressed as part of the V1.01 UI refactor, demonstrating that the post-mortem process feeds back into project planning.

### What Went Wrong

The PR #22 review identified that bundled skill file changes referenced paths that did not exist in the repository (`docs/adrs/`, `docs/projects/`, `docs/ENG-DESIGN.md`). These incorrect references were introduced when skill files were added to the project creation PR without verifying path accuracy. While caught in review, the skill files would have produced incorrect results if invoked before correction.

| Issue | Contributing Factors | Category |
|-------|---------------------|----------|
| Skill files bundled in PR #22 referenced non-existent `docs/` paths | Skill file changes were bundled into the project creation PR without verifying that path references matched the actual repository structure; no automated path validation exists for skill files | Tooling |

### Recommendations

Actionable improvements for future projects, highest priority first.

| Priority | Recommendation | Issue |
|----------|---------------|-------|
| Medium | Add an exit criteria verification step to the finish-issue skill so criteria are formally checked off during implementation, not deferred to the post-mortem | [#29](https://github.com/travisfrels/celebi/issues/29) |
| Low | Verify that path references in skill files resolve to actual repository files/directories when modifying skills, to prevent silent failures from incorrect paths | [#30](https://github.com/travisfrels/celebi/issues/30) |
